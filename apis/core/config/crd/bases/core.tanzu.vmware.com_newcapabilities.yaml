---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.9.2
  creationTimestamp: null
  name: newcapabilities.core.tanzu.vmware.com
spec:
  group: core.tanzu.vmware.com
  names:
    kind: NewCapability
    listKind: NewCapabilityList
    plural: newcapabilities
    singular: newcapability
  scope: Cluster
  versions:
  - name: v1alpha2
    schema:
      openAPIV3Schema:
        description: NewCapability is the Schema for the newcapabilities API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: NewCapabilitySpec defines the desired state of NewCapability
            properties:
              gvks:
                description: Foo is an example field of NewCapability. Edit newcapability_types.go
                  to remove/update
                items:
                  properties:
                    apiVersion:
                      type: string
                    kind:
                      type: string
                    name:
                      type: string
                    namespace:
                      type: string
                  required:
                  - apiVersion
                  - kind
                  - name
                  type: object
                type: array
            type: object
          status:
            description: NewCapabilityStatus defines the observed state of NewCapability
            properties:
              present:
                type: boolean
            required:
            - present
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
